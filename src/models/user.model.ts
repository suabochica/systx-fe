export default interface User {
    id: string,
    name: string,
    username: string,
    password: string,
    balance: number
}

export default interface UserInput {
    name: string,
    username: string,
    password: string,
    balance: number
}
